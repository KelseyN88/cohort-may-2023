// Your code here, reference the instructions if you're unsure how to start

let menu = {
    burger: 5.55,
    fries: 3.50,
    shake: 1.11,
    salad: 4.25
  }
  
  function order(orderPlaced) {
    let orderSplit = []
    orderSum = 0
  
    for (item of orderSplit) {
      orderSum = orderSum + this.price
      orderSplit.push()
    }
  }

console.log(order(menu.burger));